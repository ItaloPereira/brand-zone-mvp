export enum ImageView {
  GRID = "grid",
  LIST = "list",
  DETAILS = "details",
}

export enum ImageGroupView {
  SINGLE = "single",
  GROUPED = "grouped",
}

export enum DialogType {
  URL = "url",
  UPLOAD = "upload",
  GENERATE = "generate",
}